## .github/workflows/ci.yml -> PR에서 도커 빌드만 확인

name: CI
on: [pull_request]
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Build Docker image
              run: |
                docker build -t devops-lab:ci .